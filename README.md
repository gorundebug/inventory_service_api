# inventory_service_api

Go module path: `github.com/gorundebug/inventory_service_api`

## Make commands

```bash
make gen-proto           # generate protobuf code
make fmt-proto           # format .proto files
```